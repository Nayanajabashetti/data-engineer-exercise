pytest>=7.0
boto3>=1.28,<2.0

# Optional / dev dependencies for full project:
# - Glue job (Spark)
pyspark>=3.4

# - Airflow DAGs and AWS operators (for local experimentation only; not required in Lambda/Glue images)
apache-airflow>=2.9,<3.0
apache-airflow-providers-amazon>=8.0
